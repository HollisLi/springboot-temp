<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.temp.biz.mapper.system.SystemMenuMapper">
    <sql id="menuResVOBaseQuery">
        select id,
               name,
               permission,
               type,
               sort,
               parent_id,
               path,
               icon,
               component,
               component_name,
               status,
               visible,
               keep_alive,
               always_show,
               create_time
        from system_menu
    </sql>

    <select id="selectByParentIdAndName" resultType="com.temp.biz.entity.system.SystemMenu">
        select *
        from system_menu sm
        where delete_flag = 0
          and parent_id = #{parentId}
          and name = #{name}
    </select>

    <select id="countByParentId" resultType="int">
        select count(*)
        from system_menu
        where delete_flag = 0
          and parent_id = #{parentId}
    </select>

    <select id="list" resultType="com.temp.biz.domain.vo.system.menu.MenuResVO">
        <include refid="menuResVOBaseQuery"/>
        where delete_flag = 0
        <if test="request.name != null and request.name != ''">
            and name like concat('%', #{request.name}, '%')
        </if>
        <if test="request.status != null">
            and status = #{request.status.value}
        </if>
        order by sort, create_time desc
    </select>

    <select id="listSimple" resultType="com.temp.biz.domain.vo.system.menu.MenuSimpleRespVO">
        select id,
               name,
               parent_id,
               type
        from system_menu
        where delete_flag = 0
          and status = 0
    </select>
</mapper>